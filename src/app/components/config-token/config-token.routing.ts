import { Route } from '@angular/router';
import { ConfigTokenComponent } from './config-token.component';

export const Routes: Route[] = [
  {
    path: '',
    component: ConfigTokenComponent,
  },
];
